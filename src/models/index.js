const productModel = require('./product.model');
const salesModel = require('./sales.model');
const salesProductModel = require('./sale_product.model');

module.exports = {
  productModel,
  salesModel,
  salesProductModel,
};